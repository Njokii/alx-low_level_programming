#include "main.h"

/**
  *more_numbers - print numbers 0-9
  *
  *Return:void
  */

void more_numbers(void)
{
	int i, k;

	for (k = 0; k < 10; k++)
	{
		for (i = 0; i < 14; i++)
		{
			if (i > 9)
			{
				_putchar(i + '0');
			if ( j == 0 && j <= )4
			{
				continue;
				_putchar(j + '0');
			}
			_putchar(j + '0');
		}
		_putchar('\n');
	}

