#include "main.h"

/**
 *puts2 - main entry point
 *@str: character prints 
 *return: always 0
 */

void puts2(char *str)
{
	int t;
	t = 0;
	while ((str[t] != '\0') && (t % 2 == 0))
	{
		_putchar(str[t]);
		t++;
	}
	_putchar('\n');
}
